import React from 'react'
import { HiOutlineSpeakerWave } from 'react-icons/hi2'

export const NativeSpeakerIcon = React.memo(HiOutlineSpeakerWave)

NativeSpeakerIcon.displayName = 'NativeSpeakerIcon'
